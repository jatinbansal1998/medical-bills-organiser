# Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# API Keys (set the one you need based on your backend choice)
# =============================================================================

# OpenAI API Key (for --backend openai, default)
OPENAI_API_KEY=your_openai_api_key_here

# OpenRouter API Key (for --backend openrouter)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# =============================================================================
# LM Studio (Local Inference)
# =============================================================================
# No API key needed for LM Studio!
# Just start LM Studio, load a vision-capable model, and use:
#   medical-sorter /path/to/docs --backend lmstudio --model "your-model-name"
#
# Default LM Studio URL: http://localhost:1234/v1
# Override with --base-url if using a different port

# =============================================================================
# Model Recommendations
# =============================================================================
# OpenAI:      gpt-4o (default), gpt-4o-mini
# OpenRouter:  openai/gpt-4o, anthropic/claude-3.5-sonnet, google/gemini-pro-vision
# LM Studio:   Use any vision-capable model (e.g., llava, bakllava)

# =============================================================================
# OCR Backend Configuration (Stage 1: Text Extraction)
# =============================================================================
# OCR_BACKEND options:
#   - paddleocr (default): Fast, free, local OCR - good for typed/printed documents
#   - llm: Uses vision LLM - best for handwritten content
#   - hybrid: PaddleOCR first, falls back to LLM for low-confidence results
#
# Install PaddleOCR: pip install paddlepaddle paddleocr
# Reference: https://github.com/PaddlePaddle/PaddleOCR
OCR_BACKEND=paddleocr

# Confidence threshold for hybrid mode (0.0-1.0)
# Pages with confidence below this will fall back to LLM
OCR_CONFIDENCE_THRESHOLD=0.7

# Language for PaddleOCR (default: en)
# Common codes: en, ch (Chinese), hi (Hindi), japan, korean
# Full list: https://github.com/PaddlePaddle/PaddleOCR/blob/main/doc/doc_en/multi_languages_en.md
OCR_LANG=en
